import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import { App } from './App';

ReactDOM.render(<App />, document.getElementById('root'))
// Connecting the HTML element root to the react component file 
// index.js and rendering the DOM. Accessing the new arrays via App.js and MusicList.js